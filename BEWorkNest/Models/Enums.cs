namespace BEWorkNest.Models
{
    public enum ApplicationStatus
    {
        Pending,
        Accepted,
        Rejected
    }

    public enum RecruitmentDecision
    {
        Pending,
        Accepted,
        Rejected
    }
}
